# Analytics Backend Environment Variables
# This is a template - DO NOT commit real credentials to git

# Database Connection
# Format: postgresql://username:password@host:port/database
# Get this from your PostgreSQL hosting (Render, Heroku, etc.)
DATABASE_URL=postgresql://user:password@host:5432/database

# Dashboard Authentication
# Change these to secure values for your deployment
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=change-this-to-strong-password

# Flask Configuration
# Keep as 'production' for deployed environments
FLASK_ENV=production

# Secret Key for Flask sessions
# Generate a random string (min 32 characters)
# Example: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-random-string-min-32-chars

# CORS Origins
# Use '*' for development, or specific domains for production
# Example: https://yourdomain.com,https://app.yourdomain.com
CORS_ORIGINS=*

# Optional: Sentry DSN for error tracking
# Uncomment and add your Sentry DSN if you want error monitoring
# SENTRY_DSN=https://...
